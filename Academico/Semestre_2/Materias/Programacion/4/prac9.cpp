#include <stdio.h>

int main(){
    int inc, i, cont=0, dec;

    printf("Ingrese un numero para imprimir triangulos decrementales: ");
    scanf("%d",&i);

    //Imprimimos en incrementeo
    for (inc == i; inc < i; inc++)
    {
        printf("*");
    }
    for (dec == i; dec < 1; dec++)
    {
        printf("*");
    }
    
    
    return 0;
}